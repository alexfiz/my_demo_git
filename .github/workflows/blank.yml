name: CI-by-Aleksei-Alekseichik

on: [ pull_request ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 

      - name: Сhecking all changed files
        id  : chfile
        uses: tj-actions/changed-file@v19

      - name: List all added files
        run : |
         for file in ${{ steps.chfile.outputs.added_files }}; do
         grep "$(printf '\t')" $files | cut -f 1 -d ':' | sort | uniq -c | sort -nr | head > output.log
         done
         
      - name: Upload report
        uses: actions/upload-artifact@v2
        with:
          path: output.log
